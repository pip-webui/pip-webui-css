/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appCssStyles.Animation', []);

    thisModule.controller('AnimationController',
        function($scope) {
            console.log(this);
            // Todo: Put page specific logic here

        }
    );

})();
