/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appMaterial.Card', []);

    thisModule.controller('CardController',
        function($scope) {
            // Todo: Put page specific logic here
        }
    );

})();
