/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appMaterial.Checkbox', []);

    thisModule.controller('CheckboxController',
        function($scope) {
            // Todo: Put page specific logic here
        }
    );

})();
